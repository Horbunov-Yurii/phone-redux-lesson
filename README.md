Крок 1. Встановлюємо необхідні бібліотеки

У терміналі виконай:

npm install @reduxjs/toolkit react-redux redux-persist

Після встановлення в package.json повинно з'явитися приблизно так:

"dependencies": {
  "@reduxjs/toolkit": "...",
  "react-redux": "...",
  "redux-persist": "..."
}
